import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    marginTop: 32,
  },

  disabledWrapper: {
    opacity: 0.6,
  },

  button: {
    width: "100%",
    paddingVertical: 16,
    borderRadius: 14,
    alignItems: "center",
    justifyContent: "center",
  },

  buttonText: {
    color: "#f0f7ff",
    fontSize: 16,
    fontWeight: "700",
    letterSpacing: 0.4,
  },
});
