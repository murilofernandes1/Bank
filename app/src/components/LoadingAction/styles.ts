import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    justifyContent: "center",
    alignItems: "center",
  },

  messageWrapper: {
    alignItems: "center",
    gap: 12,
  },

  message: {
    fontSize: 22,
    fontWeight: "600",
    color: "#e0f2ff",
    textAlign: "center",
  },
});
