import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  error: {
    width: 56,
    height: 64,
    borderRadius: 12,
    borderWidth: 2,
    borderColor: "red",
    justifyContent: "center",
    alignItems: "center",
  },
  input: {
    width: 56,
    height: 64,
    borderRadius: 12,
    borderWidth: 2,
    borderColor: "#e0f2ff",
    justifyContent: "center",
    alignItems: "center",
  },
});
