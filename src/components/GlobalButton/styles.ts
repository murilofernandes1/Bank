import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  button: {
    marginTop: 40,
    width: "100%",
    backgroundColor: "#1b263b",

    paddingVertical: 18,
    borderRadius: 12,
    alignItems: "center",
  },
  text: {
    color: "#f0f7ff",
    fontSize: 16,
    fontWeight: "600",
  },
});
